# <page-name>

## 页面目标
- `<page-goal>`

## 表单字段
| 字段 | 组件 | 前端校验 |
|---|---|---|
| `<field>` | `<component>` | `<validation-rule>` |

## 交互流程
1. 输入与前端校验。
2. 调用 `<store-action>`。
3. 成功后 `<success-action>`。
4. 失败后 `<error-action>`。

## 失败提示与重试
- `<error-display-rule>`
- `<retry-rule>`

## [DOC-GAP]
- `<gap-item>`
