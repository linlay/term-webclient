# 组件设计

## 组件分层
- 页面层：`<page-layer-rule>`
- 容器层：`<container-layer-rule>`
- 展示层：`<presentational-layer-rule>`

## 组件树
```text
<Page>
  └── <Section>
       ├── <WidgetA>
       └── <WidgetB>
```

## 复用与解耦规则
- 复用规则：`<reuse-rule>`
- 解耦规则：`<decoupling-rule>`
- 命名规则：`<component-naming-rule>`

## [DOC-GAP]
- `<gap-item>`
