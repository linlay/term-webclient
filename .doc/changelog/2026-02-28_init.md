# 2026-02-28 文档基线初始化

## 变更类型
- `init`: 创建 `.doc/` 设计文档体系

## 覆盖范围
- 根文档：`README`, `GLOSSARY`
- 架构层：`architecture/*`
- 契约层：`api/*` 与 `api/modules/*`
- 后端层：`backend/*` 与 `backend/modules/*`
- 前端层：`frontend/*` 与 `frontend/pages/*`

## 与现网实现一致性声明
- 本次文档以当前仓库代码事实为准：
- API 前缀是 `/webapi` 与 `/appapi`（前端代理为 `/term/api` 与 `/appterm/api`）
- 错误响应主模式为 HTTP + `{"error":"..."}`
- 当前无 RDBMS，存储为本地文件 + 内存态

## 已标注 [DOC-GAP]
1. 错误码体系尚未代码化，仅有建议区间。
2. 部分前端类型与后端返回字段存在细微差异（如 SSH credential `updatedAt`）。
3. 列表分页协议未统一。

## 维护规则
1. 任何接口字段变更必须同步更新 `api/modules/*` 与前后端实现文档。
2. 任何新增模块必须补齐三层映射：
- `api/modules/<module>.md`
- `backend/modules/<module>.md`
- `frontend/pages/<module>.md`
3. 每次文档更新必须新增 changelog 条目。
