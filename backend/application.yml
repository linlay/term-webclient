# External deployment overrides for backend.
# This file is imported by src/main/resources/application.yml with higher precedence.

terminal:
  cli-clients:
    - id: codex
      label: Codex
      command: codex
      args: []
      workdir: .
      env: {}
      pre-commands:
        - export https_proxy="http://127.0.0.1:8001"
        - export HTTPS_PROXY="http://127.0.0.1:8001"
      shell: /bin/zsh
    - id: claude
      label: Claude Code
      command: claude
      args: []
      workdir: .
      env: {}
      pre-commands: []
      shell: /bin/zsh
  auth:
    enabled: true
    username: admin
    password-hash: 0e7517141fb53f21ee439b355b5a1d0a
    session-ttl-seconds: 43200
  ssh:
    credentials-file: data/ssh-credentials.json
    master-key: 123456
