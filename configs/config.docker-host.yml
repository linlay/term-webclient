terminal:
  default-command: bash
  default-args:
    - -l
  default-workdir: /workspace
  workdir-browse-root: /workspace
  files:
    enabled: true
    default-root-scope: SESSION_WORKDIR
    allow-outside-root: false
    allowed-roots: []
