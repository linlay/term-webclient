# Optional development overrides loaded via CONFIG_PATH.
# Only include structured values that should differ from embedded defaults.

terminal:
  files:
    enabled: true
  cli-clients:
    - id: codex
      label: Codex
      command: codex
      args: []
      workdir: .
      env: {}
      shell: /bin/zsh
    - id: claude
      label: Claude Code
      command: claude
      args: []
      workdir: .
      env: {}
      shell: /bin/zsh

assist:
  enabled: true
  # Keep /v1 in base-url so requests resolve to /v1/chat/completions.
  base-url: https://api.babelark.com/v1
  api-key: ${ASSIST_API_KEY:}
  model: Qwen3.5-397B-A17B
  timeout-seconds: 30
  max-screen-text-chars: 500
  debug-log: false
